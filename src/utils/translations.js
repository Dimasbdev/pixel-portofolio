// Lightweight modular i18n dictionary for English (EN) and Bahasa Indonesia (ID)
// Strictly ZERO EMOJIS across all languages

import { en } from '../locales/en';
import { id } from '../locales/id';

export const TRANSLATIONS = {
  en,
  id,
};
